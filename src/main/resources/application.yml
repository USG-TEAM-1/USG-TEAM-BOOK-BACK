spring:
  data:
    redis:
      host: localhost
      port: 6379
  profiles:
#    active: ${ENVIRONMENT}
    active: local

  cloud:
    gcp:
      storage:
        credentials:
          location: classpath:${GCS_KEY}.json
        project-id: ${GCS_ID}
        bucket: ${BUCKET_NAME}

open:
  library:
    key: ${LIBRARY_KEY}